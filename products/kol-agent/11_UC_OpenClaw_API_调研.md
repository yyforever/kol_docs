# 11 UC / UCloud OpenClaw API 调研

> 状态：工作材料 v0.1
> 更新：2026-04-23
> 用途：沉淀 `UC / UCloud` 托管能力与控制 API 的调研过程
> 当前生效版若已 promotion，请以 `kol_brain` 中对应 canonical 页面为准

---

## 调研目标

- 了解 `UC / UCloud` 的 `OpenClaw` 托管能力是否支持当前 prototype
- 识别后续 `OP` 控制台可能能调用哪些控制 API
- 识别哪些点还只是未知，不应提前写成结论

## 当前已知

- 当前方向已定：底层运行时使用 `UCloud` 托管 `OpenClaw`
- 需要继续调研其对 API、迁移、对象存储和后续实例接管的支持程度
- `OP` 侧首期动作范围暂不拍板，等这份调研结果收口

## 候选调研项

- 实例创建、重启、销毁、查询
- 版本升级或镜像更新
- 控制台可视化配置与 API 是否一致
- 是否存在官方迁移能力
- 对象存储接入能力
- 网络、鉴权、内网访问和外部暴露边界

## 原始发现

### 已有材料

- 旧材料认为 `UCloud` 是当前最值得优先看的底座
- `07_OpenClaw 标准化实例接管方案.md` 中收集过一批 `UCloud` 相关链接与推断
- 但这些内容混有旧架构假设，不能直接当成当前结论

### 当前待补

- ToDo：扫 `https://www.ucloud.cn/site/active/openclaw` 及相关文档
- ToDo：确认是否有可直接用于 `OP` 控制台的控制 API
- ToDo：确认是否存在官方迁移能力
- ToDo：确认对象存储、实例管理和网络边界相关能力

## 未知点

- 哪些动作能直接通过 API 做
- 哪些动作只能通过控制台或人工流程完成
- 是否支持本期原型所需的最小治理闭环

## 结论占位

- ToDo：待调研完成后补
